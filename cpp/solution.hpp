#pragma once

/* Put the necessary #include statements for your solution below to expose them
 * to the entrypoint driver.
 *
 * The driver expects the following methods and functions:
 *    - Constructor for CircularRobot.
 *    - Constructor for RectangularRobot.
 *    - Constructor for CircularObstacle.
 *    - Constructor for RectangularObstacle.
 *    - A method headingTo(const Collider&) accessible through robot classes.
 *    - A function isColliding(const Collider&, const Collider&)
 *
 * The headingTo() function should return a std::pair<double, double> where
 * the first and second values represent distance and heading respectively.
 */


